{
  # In-buffer Markdown rendering (MeanderingProgrammer/render-markdown.nvim),
  # matching the host Neovim setup. web-devicons (enabled elsewhere) supplies
  # the icons; treesitter markdown/markdown_inline parsers are already present.
  plugins.render-markdown = {
    enable = true;
  };
}
